{
  "scripts": {
    "tailwind:build": "tailwindcss -i ./src/css/input.css -o ./assets/style.css --minify",
    "tailwind:watch": "tailwindcss -i ./src/css/input.css -o ./assets/style.css --watch",
    "server": "npm run tailwind:watch & cargo run",
    "build": "npm run tailwind:build && cargo build"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.2",
    "tailwindcss": "^3.1.2"
  }
}
